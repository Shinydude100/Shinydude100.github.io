🧪 Improve test coverage for MemoryBackplane

🎯 **What:** The `MemoryBackplane.animate` method was entirely untested, along with `prefersReducedMotion` conditions and event listeners (`resize`, `visibilitychange`).
📊 **Coverage:** Now we test `MemoryBackplane.animate`, glitched node interaction state, character resetting, animation speed handling, event listener updates, and initialization branches. Lines coverage on `memory-backplane.js` has gone from 81.11% to 100%.
✨ **Result:** The codebase is now safer to refactor since the testing gap for the complex math rendering animations in `memory-backplane.js` is fully covered.
