🎯 **What:** Removed lingering Python patch scripts (`patch_html.py`, `patch_css.py`, `patch_html_header.py`, `patch_palette.py`) from the repository.

💡 **Why:** These scripts were clearly one-off utilities designed to modify the static HTML and CSS files and handle merge conflicts in markdown. They are not part of the active runtime, build pipeline, or test suite. Leaving them in the repository introduces unnecessary clutter and potential confusion about their role. Removing them improves overall repository cleanliness and maintainability.

✅ **Verification:**
- Verified that these scripts were not referenced in `package.json` scripts or any GitHub Actions workflows.
- Ran the full test suite (`pnpm test`) and HTML validation (`npx htmlhint index.html`) to confirm no regressions.

✨ **Result:** A cleaner repository with fewer loose, unused utility files, reducing technical debt and improving readability for future maintainers.
