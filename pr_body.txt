What:
Added an `llms.txt` file and converted the simple `mailto:` contact link into an interactive HTML form enriched with WebMCP declarative annotations.

Why:
To establish native AI-friendly context discovery and structured interactions. The `llms.txt` file provides automated discovery for the site structure and design system. The WebMCP annotations on the contact form allow AI agents acting on behalf of the user to natively parse and auto-fill inquiries based on the site's explicit instructions (`tooldescription`, `toolparamtitle`, etc.).

Verification:
- Created the required `llms.txt` file mapped to standard directories.
- Refactored the anchor tag into an HTML form implementing `toolname="contactForm"`, `tooldescription`, and input-level tool parameters.
- Verified visual integrity and responsive behavior using a custom Playwright CUJ script, retaining the intended pseudo-terminal aesthetics.
- HTML validation (`npx htmlhint`) passes successfully.
- Test suites (`pnpm test`) run entirely green.

Result:
A more discoverable and interoperable static portfolio site natively ready for LLM-driven interactions and context parsing.
